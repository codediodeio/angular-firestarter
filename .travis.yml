language: node_js
node_js:
  - "8.11.1"

branches:
  only:
    - master

before_script:
  - npm install -g firebase-tools
  - npm install -g @angular/cli

script:
  - ng build --prod

deploy:
  provider: firebase
  token:
    secure: $FIREBASE_TOKEN_SECURE

notifications:
  email:
    on_failure: change
    on_success: change