// const wp = require('@cypress/webpack-preprocessor')

module.exports = (on) => {
    // Uncomment for TS support
    // const options = {
    //     webpackOptions: require('../webpack.config'),
    // }
    // on('file:preprocessor', wp(options))
}